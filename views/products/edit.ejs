<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>
<body>
    <h1>Edit Product</h1>
    <p><%= products.id %></p>
    <form action="/products/<%= products._id %>?_method=PUT" method="post">
        <label for="name">
            Edit name:
            <input type="text" name="name" id="name" value="<%= products.name %>">
        </label>
        <label for="price">
            Edit price: 
            <input type="number" name="price" id="price" value="<%= products.price %>">
        </label>
        <label for="sg">Edit Category</label><select name="category" id="sg">
            <% for(let cate of categories) { %>
                <option value="<%= cate %>"<%= products.category === cate ? 'selected': '' %>><%= cate %></option>
                <% } %>
        </select>
        <button>edit</button>
    </form>
    <a href="/products">Get Back!</a>
</body>
</html>